import styled from 'styled-components/macro';
import tw from 'twin.macro';

export const StyledWrapper = styled.div`
  ${tw`py-1 h-[27px] px-3 rounded-full bg-primary text-white flex items-center text-[14px] font-semibold shadow-[0 3px 6px rgba(242, 136, 75, 0.27)]`}
`;
