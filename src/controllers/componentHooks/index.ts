import useProductCard from './productCard';
import useLayoutsControllers from './layouts';

const useComponentHooks = () => {
  return {
    useProductCard,
    useLayoutsControllers,
  };
};

export default useComponentHooks;
